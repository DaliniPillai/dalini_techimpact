function setup() {
  createCanvas(400, 500);//created canvas shape
  background(255); //created a background 
}

function draw() {
  noStroke(); //no stroke on the circle
  fill(0);
  ellipse(300,50,50,50)
  
}